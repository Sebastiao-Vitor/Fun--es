//  Operador de String (String operator)

// comparison (comparação)
console.log( 'a' == 'a')

// concatenation (comcatenação)
// Retorna a união de duas Strings
let alpha = 'alpha' 
alpha += 'bet'
console.log(alpha + 345)